
#ifndef DOMINANT_H
#define DOMINANT_H
#include "../Pixel.h"
#include <vector>
using namespace std;

vector< vector < Pixel > >  applyDominantColour(vector< vector < Pixel > > &imageVector);

#endif // DOMINANT_H